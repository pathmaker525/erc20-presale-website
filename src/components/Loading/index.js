import "./style.scss"

const Loading = () => (
  <div className="loading">
    <div className="loading-wrapper container flex">
      <span>Loading</span>
    </div>
  </div>
)

export default Loading
